import React from "react";
import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div className="container">
      <Typography variant="h3">Welcome to LoginApp</Typography>
    </div>
  );
};

export default Home;
